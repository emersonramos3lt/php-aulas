<?php
    // Aritmetic operators
    // + - * / ** %

    // Increment/decrement operators
    // ++, --

    // Operator Precedence
    // ()
    // **
    //  * / %
    // + -


    /*
    // ------------ ARITMETIC OPERATORS ------------
    $x = 10;
    $y = 2;
    $z = null;

    //$z = $x * $y; // Multiplicação *
    //$z = $x / $y; // Divisão /
    //$z = $x ** $y; // Elevado **
    // $z = $x % $y; // Acha o restante da divisão %
    echo $z;
    */

    /*
    // ----- INCREMENT/DECREMENT OPERATORS --------

    $counter = 0;

    // $counter++; // Incrementa 1 há $counter
    $counter+=3; // Incrementa 3 há $counter
    echo $counter;
    */

    // ---------- OPERATOR PRECEDENCE -------------

    // Usado mais em contas mais complexas, e é seguindo uma ordem para a conta ocorrer:
    // 1. () O que está dentro das ()
    // 2. **
    // 3. * / %
    // 4. + -

    $total = 1 + 2 - 3 * 4 / 5 ** 5;
    echo $total;








    /*
    $x = 10;
    $y = 2;
    $z = null; // $z não tem valor, o seu novo valor será a partir da soma de $x + $y

    $z = $x + $y;
    echo $z // Mostrará na página o valor de $z
    */
?>